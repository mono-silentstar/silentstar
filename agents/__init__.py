"""
Agents — specialized tasks that run against the database.

  runner.py        — base agent interface
  maintenance.py   — compiles events into fragments (needs Claude API)
  file_ingest.py   — reads files and ingests them as events or fragments
"""
